enum SignUpStatus { initial, loading, success, failure }

enum SignInStatus { initial, loading, success, profileRequired, failure }

enum UserInfoStatus { initial, loading, success, failure }

enum LinkedAccountStatus { initial, loading, success, failure }

enum Gender { male, female, orther }

enum AppAuthProvider { email, google }

enum WordStatus { stillLearning, known }

enum LearningStatus { initial, loading, success, failure, inProgress, complete }

enum AppThemeMode { light, dark, pink }
